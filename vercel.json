{
  "version": 2,
  "builds": [
    {
      "src": "api/route.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "15mb",
        "includeFiles": "api/**"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/route.py",
      "methods": ["GET", "POST", "OPTIONS"]
    }
  ]
}
